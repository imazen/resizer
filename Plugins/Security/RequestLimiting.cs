using System;
using System.Collections.Generic;
using System.Text;

namespace ImageResizer.Plugins.RequestLimiting {
    /// <summary>
    /// Prevents DOS attacks by refusing requests that don't fall inside the configured limits. 
    /// </summary>
    public class RequestLimiting {
    }
}
